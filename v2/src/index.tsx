import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import VisualiserApp from './VisualiserApp';

ReactDOM.render(
  <React.StrictMode>
    <VisualiserApp />
  </React.StrictMode>,
  document.getElementById('root')
);